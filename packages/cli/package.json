{
  "name": "@proofstack/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "proofstack": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@proofstack/core": "workspace:*",
    "commander": "^14.0.0",
    "execa": "^9.6.0",
    "playwright": "^1.53.1",
    "yaml": "^2.8.0"
  }
}
