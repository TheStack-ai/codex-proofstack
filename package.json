{
  "name": "codex-proofstack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "demo:generate": "node scripts/generate-demo-assets.mjs",
    "demo:verify": "node test/integration/demo.test.mjs",
    "test": "pnpm --filter @proofstack/core build && pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "biome check .",
    "format": "biome check --write .",
    "verify": "pnpm lint && pnpm test && pnpm typecheck && pnpm demo:generate && pnpm demo:verify && pnpm build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4",
    "@types/node": "^24.0.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
